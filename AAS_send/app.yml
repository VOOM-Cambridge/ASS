version: "2"

services:
  app:
    build: .
    privileged: true
    volumes:
      #- /etc/localtime:/etc/localtime:ro
      - ./data:/app/data
    ports:
      - "5000:5000"
    # devices:
    #   - /dev/input:/dev/input:ro
